{
  "private": true,
  "workspaces": [
    "ui",
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && yarn build && cd .. && cd server && yarn build && cd .. && cd ui && yarn build && echo donee"
  }
}
